using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class propCheck : MonoBehaviour
{
    // this is just to check if the bar has the same length as the instruction one
    public int barlength;
    public string color;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
