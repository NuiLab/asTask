using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Rendering;

public class propCheck : MonoBehaviour
{
    // this is just to check if the bar has the same length and color as the bar that is required by the instructions. This needs to be on the invisible bars(instruction bars) and on the one to build
    public int barlength;
    public string color;

    public 
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
